<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ZombieSlayer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Controller.MainMenuController"></activity>
        <activity
            android:name=".Views.LoadView"
            android:screenOrientation="landscape" />
        <activity
            android:name=".Views.SettingsView"
            android:screenOrientation="landscape" />
        <activity
            android:name=".Views.SaveStateView"
            android:screenOrientation="landscape" />
        <activity
            android:name=".Controller.GameController"
            android:screenOrientation="landscape" />
        <activity
            android:name=".Views.MainMenuView"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>